// Stub inerte de 'server-only' para Vitest: el paquete real lanza en el
// runtime default (node/jsdom). En producción Next.js resuelve la condición
// react-server y es un no-op, que es el comportamiento que simulamos aquí.
export {};
